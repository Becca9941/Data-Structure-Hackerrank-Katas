const assert = require('chai').assert;
const expect = require('chai').expect;

describe('Setup test', function() {
  it('should pass and fail as expected', function() {
    expect(true).to.equal(true);
  });
});
